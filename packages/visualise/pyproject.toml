[project]
name = "visualise"
version = "0.1.0"
description = "A very basic streamlit visualiser to try out the models"
readme = "README.md"
authors = [
    { name = "dstilesr" }
]
requires-python = ">=3.14"
dependencies = [
    "models",
    "streamlit>=1.63.0,<2",
]

[tool.uv.sources]
models = { workspace = true }
