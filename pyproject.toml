[project]
name = "cistercian-num"
version = "0.1.0"
description = "Train models to read Cistercian numbers"
readme = "README.md"
authors = [
    { name = "dstilesr", email = "d.stiles.r@hotmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3,<4",
    "numpy>=2,<3",
]

[dependency-groups]
dev = [
    "jupyterlab>=4.6.3",
]
